{
  "name": "vone",
  "version": "0.0.1",
  "description": "FIrst version of the tools that we want to build for ecommerce on mobile",
  "scripts": {
    "start": "concurrently \"npm run server\" \"cd client && npm start\"",
    "start-dev": "concurrently \"npm run server-dev\" \"cd client && npm start\"",
    "server": "babel-node index.js",
    "server-dev": "nodemon index.js",
    "build":"cd client && npm run build"
  },
  "author": "Adrien Dulong",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "babel-register": "^6.18.0",
    "express": "^4.14.0",
    "express-graphql": "^0.6.1",
    "graphql": "^0.8.2"
  },
  "devDependencies": {
    "concurrently": "^3.1.0",
    "foreman": "^2.0.0"
  }
}
